import { DocumentStatus } from '@beavr/types';

export const DOC_EXPIRATION_TIME_MILLISECONDS = 1 * 365 * 24 * 60 * 60 * 1000; // 1 year in millis
export const DOC_DEFAULT_STATUS: DocumentStatus = 'IN_PROGRESS';
